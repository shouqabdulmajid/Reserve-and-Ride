name: frontend_flutter
description: A simplified Tashilat frontend using Flutter

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # === الإضافة الضرورية لدعم اللغة العربية والـ RTL ===
  flutter_localizations:
    sdk: flutter
  # ===================================================

  http: ^1.1.2
  image_picker: ^1.1.2
  
  # ✅ تم تصحيح هذا القيد ليتوافق مع متطلبات flutter_localizations
  intl: ^0.20.2 
  
  qr_flutter: ^4.1.0
  qr_code_scanner: ^1.0.1
  
  # ✅ تمت إضافة حزمة flutter_svg المطلوبة لعرض الأيقونة
  flutter_svg: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.13.1"

flutter:
  uses-material-design: true
  
  # 🛑 تأكد من إنشاء مجلد "assets" في جذر المشروع ووضع الملفات التالية بداخله.
  assets:
   - assets/images/
   - assets/seat_icon.svg # المسار المطلوب لملف أيقونة المقعد

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/icon.png"
  min_sdk_android: 21